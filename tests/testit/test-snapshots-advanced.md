`````{r}
cat("Use `code` for inline code\n")
cat("````{r} for R code blocks\n")
`````

`````
Use `code` for inline code
````{r} for R code blocks
`````

`````{r}
e <- new.env()
print(e)
`````

`````
<environment: ...>
`````
